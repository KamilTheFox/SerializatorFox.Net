# 📊 Benchmarks

Сравнение **SerializatorFox** с популярными сериализаторами на .NET 8.

> **Среда:** 12th Gen Intel Core i5-12450H 2.00GHz · .NET 8.0.11 · Windows 11 24H2

---

## 📦 Размер payload

| Сериализатор | Байт | Сравнение с Fox compressed |
|---|---:|---:|
| 🦊 **Fox (compressed)** | **885** | — |
| ⚡ MessagePack (LZ4) | 1 088 | +23% |
| 🦊 Fox (no compression) | 1 591 | +80% |
| ⚡ MessagePack (no compression) | 1 801 | +104% |
| 📄 Newtonsoft.Json | 2 230 | +152% |

Fox в сжатом режиме производит **наиболее компактный** результат среди всех участников.

---

## ⚡ Сериализация

| Метод | Среднее | Погрешность | Аллокации | Место |
|---|---:|---:|---:|:---:|
| MessagePack (no compression) | 1.768 µs | ±0.021 µs | 1.79 KB | 🥇 |
| MessagePack (LZ4) | 3.463 µs | ±0.025 µs | 1.14 KB | 🥈 |
| Newtonsoft.Json | 11.445 µs | ±0.593 µs | 10.66 KB | 🥉 |
| 🦊 **Fox (no compression)** | **14.830 µs** | ±0.293 µs | **10.54 KB** | 4 |
| 🦊 Fox (compressed) | 42.416 µs | ±0.834 µs | 12.20 KB | 5 |

---

## 📥 Десериализация

| Метод | Среднее | Погрешность | Аллокации | Место |
|---|---:|---:|---:|:---:|
| MessagePack (no compression) | 3.022 µs | ±0.032 µs | 3.04 KB | 🥇 |
| MessagePack (LZ4) | 3.718 µs | ±0.063 µs | 3.09 KB | 🥈 |
| 🦊 **Fox (no compression)** | **10.350 µs** | ±0.195 µs | **9.70 KB** | 🥉 |
| 🦊 Fox (compressed) | 17.461 µs | ±0.348 µs | 14.18 KB | 4 |
| Newtonsoft.Json | — | — | — | — |

> Newtonsoft.Json при десериализации упал с ошибкой (вероятно конфликт с Windows Defender при запуске дочернего процесса). Десериализация без сжатия у Fox теперь замерена корректно.

---

## 🗺️ Дорожная карта

### v2.0 — Source Generators (устранение рефлексии)

Текущая версия использует рефлексию с кэшированием — это главная причина отставания от MessagePack в 8–10 раз. Переход на Source Generators позволит генерировать код сериализации на этапе компиляции.

**Что генерируется:**

Для каждого типа, помеченного атрибутом (например `[FoxSerializable]`), Source Generator создаёт реализацию интерфейса `IFoxSerializable`:

```csharp
interface IFoxSerializable {
    void Serialize(Stream stream);
    void Deserialize(Stream stream);
}
```

```csharp
// Генерируется автоматически во время компиляции
partial class MyDto : IFoxSerializable {
    public void Serialize(Stream stream) {
        stream.Write(0xA3F2C1B4); // MurmurHash от "Value" — константа на этапе компиляции
        stream.Write(this.Value);
        // ...
    }

    public void Deserialize(Stream stream) {
        uint fieldHash = stream.ReadUInt32();
        // сгенерированный switch по хэшам полей
    }
}
```

MurmurHash каждого поля вычисляется **один раз при компиляции** и вшивается как числовая константа — никаких вычислений в рантайме.

---

### Полиморфизм без рефлексии — TypeRegistry

Для поддержки полиморфных типов генерируется глобальный реестр:

```csharp
// Тоже генерируется Source Generator'ом
static class FoxTypeRegistry {
    public static IFoxSerializable Create(uint typeHash) => typeHash switch {
        0xA3F2C1B4 => new TypeA(),
        0x91E4D2A7 => new TypeB(),
        _ => throw new UnknownFoxTypeException(typeHash)
    };
}
```

При сериализации полиморфного поля в поток пишется MurmurHash имени типа (4 байта). При десериализации он читается и через `FoxTypeRegistry` создаётся нужный экземпляр — без рефлексии, без словарей в рантайме.

**Сравнение с MessagePack Union:** MessagePack требует вручную задавать числовые индексы (`[Union(0, typeof(TypeA))]`) и следить за их уникальностью. Fox использует хэш от имени типа — индексы не нужны, новые типы добавляются без риска сломать совместимость. Единственный edge case — переименование типа меняет хэш, решается атрибутом `[FoxTypeName("OriginalName")]`.

---

### v2.1 — замена GZip на LZ4

GZip оптимизирован под степень сжатия, LZ4 — под скорость. Текущие накладные расходы на сжатие составляют ~27 мкс при сериализации и ~7 мкс при десериализации. LZ4 даст примерно 2–4 мкс — то есть Fox compressed будет почти не отличаться по скорости от Fox без сжатия, оставаясь при этом компактнее.

---

## 📈 Прогноз после v2.0 + v2.1

Оценки приблизительные, основаны на характеристиках аналогичных Source Generator реализаций.

### Размер payload — уже сейчас лучший результат, останется таким же

| Сериализатор | Байт |
|---|---:|
| 🦊 **Fox (compressed)** | **~885** |
| ⚡ MessagePack (LZ4) | 1 088 |
| ⚡ MessagePack (no compression) | 1 801 |
| 📄 Newtonsoft.Json | 2 230 |

### Сериализация — прогноз

| Метод | Сейчас | Прогноз v2 |
|---|---:|---:|
| MessagePack (no compression) | 1.768 µs | — |
| MessagePack (LZ4) | 3.463 µs | — |
| 🦊 Fox (no compression) | 14.830 µs | **~2–3 µs** |
| 🦊 Fox (compressed, LZ4) | 42.416 µs | **~4–6 µs** |

Fox без сжатия выйдет на уровень MessagePack. Fox с LZ4 будет немного медленнее из-за компрессии, но останется компактнее.

### Десериализация — прогноз

| Метод | Сейчас | Прогноз v2 |
|---|---:|---:|
| MessagePack (no compression) | 3.022 µs | — |
| MessagePack (LZ4) | 3.718 µs | — |
| 🦊 Fox (no compression) | 10.350 µs | **~3–5 µs** |
| 🦊 Fox (compressed, LZ4) | 17.461 µs | **~5–8 µs** |

### Итоговые преимущества перед конкурентами

**Перед Newtonsoft.Json и System.Text.Json:**
- Бинарный формат vs текстовый — принципиально меньший размер payload
- Скорость сериализации на порядок выше
- Нет overhead на экранирование строк, парсинг UTF-8, поиск ключей

**Перед MessagePack:**
- Компактнее на ~20% в сжатом виде (885 vs 1088 байт) — за счёт MurmurHash вместо строковых ключей
- Полиморфизм без ручного управления индексами — просто добавь тип и он автоматически получит хэш
- Нет риска сломать совместимость при добавлении новых типов в иерархию
- Атрибут `[FoxTypeName]` защищает от потери совместимости при рефакторинге

**Где Fox будет проигрывать даже после v2:**
- Экосистема и документация — MessagePack существует много лет
- Интеграции (ASP.NET formatters, gRPC и т.д.) — придётся писать самому
- Скорость на очень простых плоских объектах — там overhead компрессии заметнее

---

## 💡 Примечания

- **Fox использует MurmurHash** для идентификации типов и полей вместо строковых имён — отсюда компактный бинарный формат.
- MessagePack использует кодогенерацию (Source Generators / IL Emit), что даёт преимущество в скорости.
- Fox работает через рефлексию с кэшированием — это основная причина разницы в скорости.
- Сжатие в Fox реализовано через GZip, отсюда дополнительные ~27 мкс при сериализации и ~7 мкс при десериализации.